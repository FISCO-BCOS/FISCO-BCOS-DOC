# FISCO BCOS 多语言 SDK 核心接口调用对比

> 任务：横向对比 Java SDK 与 C++ SDK 调用 `getBlockNumber` 和 `sendTransaction` 核心接口的代码实现差异。  
> 报告人：[你的名字]  
> 日期：2026-08-29


## 引言
FISCO BCOS 提供了多种语言的 SDK，方便不同技术栈的开发者接入区块链网络。其中 **Java SDK** 和 **C++ SDK** 是最常用、功能最完整的两种。对于开发者而言，了解两者在核心接口调用上的异同，有助于：
- 根据项目语言选型快速上手
- 理解 SDK 底层设计思路
- 在跨语言协作时减少沟通成本
本文以两个最基础的 RPC 接口为例：
- **`getBlockNumber`**：查询最新区块高度
- **`sendTransaction`**：发送交易（以部署合约为例）
从 **配置加载、代码风格、对象构建、异常处理** 四个维度，对 Java SDK 和 C++ SDK 的调用方式进行详细对比。

## 环境准备
节点准备
在进行 SDK 调用前，需确保已有一个 FISCO BCOS 节点运行，并开启 RPC 服务。假设节点配置如下：
| 配置项 | 值 |
|--------|-----|
| 节点 IP | `127.0.0.1` |
| RPC 端口 | `8545` |
| 群组 ID | `group0` |
| 加密类型 | 非国密（Secp256K1） |
SDK 依赖
**Java SDK**（基于 Gradle）：
```gradle
dependencies {
    implementation 'org.fisco-bcos:java-sdk:3.3.0'
}

##接口一：getBlockNumber
import org.fisco.bcos.sdk.client.Client;
import org.fisco.bcos.sdk.config.ConfigOption;
import org.fisco.bcos.sdk.config.exceptions.ConfigException;
import org.fisco.bcos.sdk.config.model.ConfigProperty;
import org.fisco.bcos.sdk.model.RpcException;

public class GetBlockNumberJava {
    public static void main(String[] args) throws ConfigException, RpcException {
        // 1. 加载配置文件
        ConfigProperty configProperty = ConfigOption.load("config.ini");
        // 2. 创建 Client 实例
        Client client = Client.build(configProperty);
        // 3. 调用 getBlockNumber（同步）
        BigInteger blockNumber = client.getBlockNumber("group0");
        System.out.println("Current block number: " + blockNumber);
        // 4. 关闭客户端
        client.stop();
    }
}
代码特点：
通过 ConfigOption.load() 静态方法加载配置
Client.build() 构建客户端实例
getBlockNumber(groupID) 直接返回 BigInteger，同步阻塞
需要显式调用 stop() 释放资源

##C++ SDK 实现
#include <bcos-cpp-sdk/SdkFactory.h>
#include <bcos-utilities/Common.h>
#include <iostream>
#include <memory>

using namespace bcos;
using namespace bcos::cppsdk;

int main(int argc, char** argv) {
    if (argc < 2) {
        std::cerr << "Usage: ./get_block_number <config> [groupID]" << std::endl;
        return -1;
    }
    std::string configFile = argv[1];
    std::string groupID = (argc >= 3) ? argv[2] : "group0";

    // 1. 创建 SDK 工厂并构建 SDK 实例
    auto factory = std::make_shared<SdkFactory>();
    auto sdk = factory->buildSdk(configFile);
    sdk->start();

    // 2. 获取 JSON-RPC 客户端并调用 getBlockNumber
    auto jsonRpc = sdk->jsonRpc();
    auto response = jsonRpc->getBlockNumber(groupID);
    std::cout << "Current block number: " << response["result"].asInt64() << std::endl;

    // 3. 停止 SDK
    sdk->stop();
    return 0;
}

##接口二：sendTransaction（部署 HelloWorld 合约）
import org.fisco.bcos.sdk.client.Client;
import org.fisco.bcos.sdk.config.ConfigOption;
import org.fisco.bcos.sdk.config.exceptions.ConfigException;
import org.fisco.bcos.sdk.model.RpcException;
import org.fisco.bcos.sdk.transaction.builder.TransactionBuilder;
import org.fisco.bcos.sdk.transaction.builder.TransactionBuilderService;
import org.fisco.bcos.sdk.transaction.model.po.RawTransaction;
import org.fisco.bcos.sdk.transaction.model.dto.TransactionResponse;

public class DeployHelloJava {
    public static void main(String[] args) throws ConfigException, RpcException {
        // 1. 加载配置
        ConfigProperty configProperty = ConfigOption.load("config.ini");
        Client client = Client.build(configProperty);

        // 2. 获取账户（从配置或本地文件）
        CryptoKeyPair cryptoKeyPair = client.getCryptoSuite().createKeyPair();

        // 3. 构造交易
        String contractBin = "608060405234801561001057..."; // 编译后的字节码
        TransactionBuilder builder = new TransactionBuilderService(client, cryptoKeyPair);
        RawTransaction rawTx = builder.createTransaction("", contractBin, "", 0);

        // 4. 发送交易（同步）
        TransactionResponse response = client.sendTransactionAndGetResponse(rawTx);
        System.out.println("Contract address: " + response.getContractAddress());

        client.stop();
    }
}
#include <bcos-cpp-sdk/SdkFactory.h>
#include <bcos-cpp-sdk/utilities/crypto/KeyPairBuilder.h>
#include <bcos-cpp-sdk/utilities/tx/TransactionBuilder.h>
#include <bcos-cpp-sdk/utilities/tx/TransactionBuilderService.h>
#include <bcos-utilities/Common.h>
#include <iostream>
#include <memory>

using namespace bcos;
using namespace bcos::cppsdk;

int main(int argc, char** argv) {
    if (argc < 3) {
        std::cerr << "Usage: ./deploy_hello <config> <groupID>" << std::endl;
        return -1;
    }
    std::string configFile = argv[1];
    std::string group = argv[2];

    // 1. 初始化 SDK
    auto factory = std::make_shared<SdkFactory>();
    auto sdk = factory->buildSdk(configFile);
    sdk->start();

    // 2. 获取群组信息，确定加密类型
    auto groupInfo = sdk->service()->getGroupInfo(group);
    if (!groupInfo) { return -1; }

    // 3. 生成密钥对
    auto keyPairBuilder = std::make_shared<KeyPairBuilder>();
    auto keyPair = keyPairBuilder->genKeyPair(
        groupInfo->smCryptoType() ? CryptoType::SM2 : CryptoType::Secp256K1
    );

    // 4. 构造并签名交易
    auto txBuilder = std::make_shared<TransactionBuilder>();
    auto txService = std::make_shared<TransactionBuilderService>(sdk->service(), group, txBuilder);
    std::string hexBin = "608060405234801561001057...";  // 合约字节码
    auto binBytes = fromHexString(hexBin);
    auto txPair = txService->createSignedTransaction(
        *keyPair, "", *binBytes.get(), "", 0
    );

    // 5. 发送交易
    std::promise<bool> p;
    sdk->jsonRpc()->sendTransaction(group, "", txPair.second, false,
        [&p](Error::Ptr err, std::shared_ptr<bcos::bytes> resp) {
            // 回调解析 contractAddress
            p.set_value(true);
        }
    );
    p.get_future().get();

    sdk->stop();
    return 0;
}

##对比分析
对比维度	                   Java SDK	                                            C++SDK
账户生成                   client.getCryptoSuite().createKeyPair()	            KeyPairBuilder::genKeyPair()
交易构造	                   TransactionBuilderService 封装	                    同样的类，但需手动传入 group 和 service
交易签名	                  自动签名（createTransaction）	                      调用 createSignedTransaction 显式签名
交易发送	                  sendTransactionAndGetResponse                       同步返回	异步回调方式，需用 promise 同步等待
获取合约地址	              response.getContractAddress()	                      从 JSON 回调中解析 contractAddress
代码量                   	较简洁（~20 行核心逻辑）	                            较冗长（~40 行，含异步转换）

##总结
通过对比 getBlockNumber 和 sendTransaction 两个核心接口在 Java SDK 和 C++ SDK 中的实现，可以看出：
两种 SDK 在配置加载、客户端构建、交易构造等核心流程上高度一致，体现了 FISCO BCOS 对多语言支持的统一设计思想。
差异主要体现在语言特性上：Java 更注重类型安全和异常机制，C++ 更注重灵活性和性能控制。
开发者可以根据项目实际需求和技术背景选择合适的 SDK，两种 SDK 均能稳定地与 FISCO BCOS 节点进行交互。

##附：相关链接
Java SDK 仓库：https://github.com/FISCO-BCOS/java-sdk
C++ SDK 仓库：https://github.com/FISCO-BCOS/bcos-cpp-sdk
官方文档（SDK 接入）：https://fisco-bcos-doc.readthedocs.io/zh-cn/release-3/docs/sdk/index.html
